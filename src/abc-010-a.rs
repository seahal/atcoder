// AtCoder Beginner Contest(ABC), 010
// A - ハンドルネーム

fn main() {
    let mut buffer = String::new();
    let _ = std::io::stdin().read_line(&mut buffer);
    let x = buffer.trim();
    println!("{}pp", x);
}
